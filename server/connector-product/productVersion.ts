/** MineuQR Connector product version — keep in sync with installer metadata. */
export {
  MINEUQR_CONNECTOR_PRODUCT_NAME,
  MINEUQR_CONNECTOR_VERSION,
} from "../connector-local/infrastructure/productIdentity";

export const MINEUQR_CONNECTOR_MIN_CLOUD_VERSION = "1.0.0" as const;
