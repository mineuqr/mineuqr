import { router } from "../_core/trpc";

/**
 * PRINT-CONNECTOR-1 legacy router — retired from production.
 * Use printWorkspace.read / printerManagement instead.
 */
export const printConnectorRouter = router({});
