/**
 * THERMAL-PRINTING-4B / 13B — re-exports shared ESC/POS types (backward compatibility).
 */
export type {
  EscPosAlign,
  EscPosCommand,
  EscPosDocument,
} from "../../shared/printing/escpos/escposTypes";
