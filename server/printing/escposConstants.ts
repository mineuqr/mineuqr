/**
 * THERMAL-PRINTING-4C / 13B — re-exports shared ESC/POS constants (backward compatibility).
 */
export {
  DEFAULT_SEPARATOR_LENGTH,
  ESC_POS_ALIGN_VALUE,
  ESC_POS_BYTES,
  ESC_POS_CUT_PARTIAL,
} from "../../shared/printing/escpos/escposConstants";
