# دليل ما بعد النشر - منيو QR

## 🚀 الخطوات الأساسية الفورية

### 1. **التحقق من الأداء والأمان**
- ✅ اختبر الموقع على أجهزة مختلفة (الهاتف، الكمبيوتر، التابلت)
- ✅ تحقق من سرعة التحميل باستخدام Google PageSpeed Insights
- ✅ اختبر نظام الدفع PayPal بكامل التدفق
- ✅ تأكد من أن جميع الروابط تعمل بشكل صحيح
- ✅ اختبر تبديل اللغة (العربية/الإنجليزية) على جميع الصفحات

### 2. **إعداد المراقبة والتحليلات**
```bash
# تفعيل تحليلات الموقع
# انتقل إلى Settings → Analytics في لوحة التحكم
# ستجد VITE_ANALYTICS_ENDPOINT و VITE_ANALYTICS_WEBSITE_ID
```

**ما يجب مراقبته:**
- عدد الزيارات اليومية
- معدل الارتداد (Bounce Rate)
- الصفحات الأكثر زيارة
- مصادر الزيارات (مباشر، جوجل، وسائل اجتماعية)

### 3. **تكوين النطاق المخصص (اختياري)**
```
Settings → Domains (أو لوحة استضافة Vercel → Domains)
- اشتر نطاقاً مخصصاً (مثل menu.example.com) أو استخدم نطاق الاستضافة الافتراضي
- ربط النطاق بالموقع
- تفعيل SSL/HTTPS (عادةً تلقائي عبر الاستضافة)
```

---

## 📊 المراقبة المستمرة (أسبوعياً)

### 1. **فحص الأداء**
```bash
# استخدم أدوات المراقبة المدمجة
Dashboard → Performance
- وقت الاستجابة
- معدل الأخطاء
- استخدام قاعدة البيانات
```

### 2. **مراجعة السجلات**
```bash
# سجلات الإنتاج (Vercel أو مزود الاستضافة)
- لوحة Vercel → Project → Logs (أخطاء الخادم وطلبات API)
- متصفح المستخدم → أدوات المطور → Console / Network (أخطاء الواجهة)
- إشعارات البريد من Resend (فشل إرسال التحقق أو إعادة التعيين)
```

### 3. **تحديث المحتوى**
- أضف عروضاً جديدة
- حدّث قائمة المطاعم
- أضف صور وفيديوهات جديدة

---

## 🔧 الصيانة الدورية (شهرياً)

### 1. **تحديث الحزم والمكتبات**
```bash
cd <مسار-المستودع-المحلي>
pnpm update
pnpm test  # تأكد من عدم كسر أي شيء
```

### 2. **نسخ احتياطي من قاعدة البيانات**
```bash
# استخدم أداة النسخ الاحتياطي في Dashboard
Dashboard → Database → Backup
```

### 3. **مراجعة الأمان**
- تحديث كلمات المرور
- مراجعة صلاحيات المستخدمين
- تحقق من الوصول غير المصرح به

---

## 🐛 استكشاف الأخطاء

### المشكلة: الموقع بطيء
**الحل:**
1. تحقق من استخدام قاعدة البيانات
2. قلل عدد الاستعلامات غير الضرورية
3. أضف caching للبيانات الثابتة
4. استخدم CDN للصور والملفات الثابتة

### المشكلة: أخطاء في الدفع
**الحل:**
1. تحقق من مفاتيح PayPal في Settings → Payment
2. راجع سجلات المتصفح (Console) وسجلات الخادم في لوحة الاستضافة
3. اختبر الدفع بطريقة الاختبار (Test Mode)
4. تأكد من تفعيل webhooks في PayPal

### المشكلة: مشاكل في الترجمة
**الحل:**
1. تحقق من ملفات الترجمة: `client/src/locales/`
2. أضف النصوص المفقودة
3. اختبر كلا اللغتين بعد التحديث

---

## 📈 تحسينات مستقبلية مقترحة

### المرحلة 1 (الشهر الأول)
- [ ] إضافة نظام تقييمات وتعليقات المستخدمين
- [ ] تحسين SEO للموقع
- [ ] إضافة خريطة موقع (Sitemap)
- [ ] تفعيل البريد الإلكتروني للإشعارات

### المرحلة 2 (الشهر الثاني)
- [ ] إضافة نظام الطلبات المباشر
- [ ] دعم لغات إضافية (الفرنسية، الإسبانية)
- [ ] تطبيق موبايل (iOS/Android)
- [ ] نظام الولاء والنقاط

### المرحلة 3 (الشهر الثالث)
- [ ] تحليلات متقدمة وتقارير
- [ ] تكامل مع أنظمة إدارة المطاعم
- [ ] نظام الحجوزات والطاولات
- [ ] دعم العملات المتعددة

---

## 🔐 قائمة التحقق الأمني

- [ ] تفعيل HTTPS على جميع الصفحات
- [ ] تحديث كلمات المرور بانتظام
- [ ] مراجعة صلاحيات المستخدمين
- [ ] تفعيل المصادقة الثنائية (2FA)
- [ ] نسخ احتياطية منتظمة من قاعدة البيانات
- [ ] مراقبة محاولات الدخول الفاشلة
- [ ] تحديث المكتبات الأمنية

---

## 📞 الدعم والمساعدة

**للمشاكل التقنية:**
- راجع سجلات Vercel (أو مزود الاستضافة) وأحداث `[OPS][AUTH]` في السجلات
- راجع [auth-ops-signals.md](docs/auth-ops-signals.md) و [deployment-auth-readiness.md](docs/deployment-auth-readiness.md)
- شغّل `pnpm test` و `pnpm check` محلياً قبل النشر

---

## 📋 جدول الصيانة الموصى به

| المهمة | التكرار | الأولوية |
|-------|---------|---------|
| فحص الأداء | يومي | عالية |
| مراجعة السجلات | أسبوعي | متوسطة |
| تحديث المحتوى | أسبوعي | عالية |
| تحديث الحزم | شهري | متوسطة |
| النسخ الاحتياطي | شهري | عالية |
| مراجعة الأمان | شهري | عالية |

---

**ملاحظة:** استخدم Git tags أو نشرات Vercel المحفوظة للرجوع إلى إصدار سابق بعد أي تحديث مهم.
