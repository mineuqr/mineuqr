# Accessibility Report

| Check | Status |
|-------|--------|
| Tablist / tabs / aria-selected | Pass |
| Breadcrumb nav label | Pass |
| Card aria-label + keyboard Enter/Space | Pass |
| Focus-visible rings | Pass |
| Empty `aria-live=polite` | Pass |
| Error `role=alert` | Pass |
| Touch targets (tabs/selects/exports ≥ 40px) | Pass |
| Reduced motion (`motion-safe`, chart `isAnimationActive`) | Pass |
| Color not sole cue (labels + captions retained) | Pass |

## Observation

Contrast relies on slate-400 labels on dark shells — acceptable for this shell; primary values use high-contrast category tints.
