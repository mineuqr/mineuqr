# QR-FREEZE-GOVERNANCE.md

Expiry MUST NOT delete, regenerate, or invalidate the persistent QR identity (CE-20, I-CE-14).

Renewal MUST restore the same restaurant/menu/QR identity without reprinting (CE-21, I-CE-15).

While FROZEN, public QR resolution MUST NOT serve the active commercial menu if the Frozen/Public Runtime policy requires suspension. It MUST resolve to the approved subscription-required experience.

This program does **not** redesign QR architecture. Frozen public resolution is follow-on **COMMERCIAL-FROZEN-ACCOUNT-STATE-1**.
