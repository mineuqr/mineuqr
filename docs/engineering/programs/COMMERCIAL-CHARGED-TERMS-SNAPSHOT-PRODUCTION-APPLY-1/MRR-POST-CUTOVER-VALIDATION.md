# MRR POST-CUTOVER VALIDATION

Schema ready. **Runtime not deployed** — do not claim Production MRR certification.

Intended after deploy: MRR = current snapshot only; ARR = MRR × 12; no Binding leftover; no catalog fallback. Empty table ⇒ no snapshot MRR contributions until a real commitment writes a row.
