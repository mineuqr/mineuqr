# ADR IMPACT

ADR-034 / 035 / 036: **no file amended** in this program.

This cutover implements the OD-1 decision (`commercial_plans.id` = canonical internal identity) on `user_subscriptions.planId` only.

A future ADR-034 addendum may record that subscription storage now holds the Live Plan UUID. That amendment is not part of this program.
