# OD-2 PRODUCTION CERTIFICATION

**PRODUCTION CERTIFIED** for subscription storage identity cutover.

`user_subscriptions.planId` is now `commercial_plans.id` (UUID).

Not certified: application deploy, OD-3, OD-4, SAFE DELETE, git commit/push.
