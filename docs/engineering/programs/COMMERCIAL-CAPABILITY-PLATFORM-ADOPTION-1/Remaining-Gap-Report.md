# Remaining Gap Report — COMMERCIAL-CAPABILITY-PLATFORM-ADOPTION-1

**Class:** Non-blocking residuals · Do not block adoption certification for the filter plane (18 keys).

---

## 1. Commercializable Discovery CAPs not yet Plan filters

Kitchen, Printing, Realtime, Device, Screen Pairing, Counter Pickup, Split Payment, Refund, CRMP, Financial Shift, AI, etc.

**Why not closed here:** Adding filter keys requires Runtime matrix expansion + Snapshot packaging + enforcement — that is a **capability commercialization program**, not SSOT wiring. Explicitly out of “no Runtime redesign.”

---

## 2. Enforcement depth

Many filter keys are `flags_only` or `coarse_legacy` (templates/colors/fonts). Runtime maps exist (I-SRE-02); not every domain API uses `requireFeature` hard gates.

**Follow-on:** Domain enforcement completeness program under I-SRE-01 exclusivity.

---

## 3. Limit DTO surface

10 limit filter keys vs 3 hard `checkLimit` keys emphasized in Runtime — pre-existing certified residual.

---

## 4. Legacy unbound bridge

`planFeatureMatrix.ts` remains for unbound subscriptions — not Catalog UI SSOT; must not grow new sellable keys.

---

## 5. Overlay scheduling persistence

Approved/Scheduled/Archived overlay remains process-local (prior publishing program residual) — unrelated to capability SSOT.
