# MRR IMPACT

Formula unchanged: qualifying × current Charged Terms monthly equivalent.

`loadChargedTermsForMrr` now loads **current snapshots** (one per subscription). Historical versions are not summed. If no snapshot row, Binding leftover is used so pre-0089 Production MRR does not drop to 0.

Still no Live Plan catalog price, no `subscription_plans`.
