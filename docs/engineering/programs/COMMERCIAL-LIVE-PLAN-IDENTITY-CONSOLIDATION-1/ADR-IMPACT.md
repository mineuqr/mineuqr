# ADR-IMPACT

No ADR edited.

| ADR | Amendment required? |
|-----|---------------------|
| 034 | **NO** — Live Plan remains catalog authority. Identity cutover is implementation, not a new constitution. |
| 035 | **NO** — price semantics unchanged. |
| 036 | **NO** — MRR unchanged. |

If AA later chooses `code` (not UUID) as the subscription FK, that is an implementation decision under 034, not an automatic ADR amend.
