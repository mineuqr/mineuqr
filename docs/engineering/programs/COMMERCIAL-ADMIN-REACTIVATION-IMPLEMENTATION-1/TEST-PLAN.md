# TEST PLAN

Primary: `server/commercial/__tests__/adminReactivation.test.ts`

Also: snapshot versioning (N+1 even when amount matches), subscriptionAudit (create CONFLICT + implicit reject), admin-auth-1e (platform block).

Regression (this program): 19 files, 191 passed.
