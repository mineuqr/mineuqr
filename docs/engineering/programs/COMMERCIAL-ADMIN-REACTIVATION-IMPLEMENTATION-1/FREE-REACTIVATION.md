# FREE REACTIVATION

`mode=free` + `freePeriod { unit, duration }` + reason.

New concession from `now`. Period end = concession `endsAt`. No snapshot. No $0 snapshot. Cancelled concessions are not restored.

Persist failure does not return success.
