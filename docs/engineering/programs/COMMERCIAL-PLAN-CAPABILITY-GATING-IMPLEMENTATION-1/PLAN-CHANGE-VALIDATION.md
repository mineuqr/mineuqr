# PLAN CHANGE VALIDATION

Capability state follows the current entitled Live Plan bundle. No capability snapshot. No Charged Terms write.

Matrix test: `menuDesign` ON → OFF → ON follows successive `resolveOwnerEntitlements` results. Existing data is not deleted by the gate.
