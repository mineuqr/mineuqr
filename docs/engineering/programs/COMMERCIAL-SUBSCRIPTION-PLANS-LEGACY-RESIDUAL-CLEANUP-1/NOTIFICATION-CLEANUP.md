# NOTIFICATION-CLEANUP

Admin create/update subscription notifications resolve the plan label through `resolveLivePlanDisplayByLegacyId`.

Webhook owner emails: same Live Plan display. Amounts stay provider facts.

Notification architecture (types, delivery) unchanged.
