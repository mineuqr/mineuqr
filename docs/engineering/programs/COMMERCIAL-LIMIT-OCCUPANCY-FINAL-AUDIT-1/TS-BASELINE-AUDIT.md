# TS BASELINE AUDIT

**Program:** COMMERCIAL-LIMIT-OCCUPANCY-FINAL-AUDIT-1  

| | Count |
|--|-------|
| TS BASELINE | 188 |
| TS CURRENT | 188 |
| TS DELTA | 0 |

`pnpm check` still exits non-zero because the 188 `error TS*` exist. Count of lines matching `error TS` = **188**.

No new TS errors from G-07 timeout alignment or the PLATFORM_OWNER hub mock. No “pre-existing” claim required.
