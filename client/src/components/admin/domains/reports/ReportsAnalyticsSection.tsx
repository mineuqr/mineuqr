import { StatisticsPanel } from "@/pages/admin/StatisticsPanel";

/** Reports domain — analytics summary (charts, KPIs, subscriber table, export). */
export function ReportsAnalyticsSection() {
  return <StatisticsPanel />;
}
