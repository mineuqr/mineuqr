/**
 * Legacy export — ownership: Health Domain (REBUILD-5F).
 * Host path unchanged; implementation lives in Health composition layer.
 */
export { HealthDiagnosticsSection as CommercialEntitlementsDiagnostics } from "@/components/admin/domains/health";
