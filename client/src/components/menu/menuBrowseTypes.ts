import type React from "react";

/** Shared font presentation tokens for browse item/offer cards. */
export type MenuBrowseFontStyles = {
  arStyle: React.CSSProperties;
  enStyle: React.CSSProperties;
  headingColor?: string;
  bodyColor?: string;
  priceColor?: string;
  headingScale?: string;
  bodyScale?: string;
  priceScale?: string;
};
